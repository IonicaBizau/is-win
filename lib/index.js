"use strict";

/**
 * isWin
 * Check if the machine is running Windows or not.
 *
 * @name isWin
 * @function
 * @param {Number} a Param descrpition.
 * @param {Number} b Param descrpition.
 * @return {Number} Return description.
 */
module.exports = function isWin (a, b) {
    return a + b;
};